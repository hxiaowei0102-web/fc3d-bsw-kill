福彩3D 百十个杀码系统 — 数据更新服务商
（2026-08-01 审计后更新）

当前存活源（2层降级，按优先级排列）：

1. 灰鸟API  【主源·唯一稳定】
   类型: JSON API
   地址: http://api.huiniao.top/interface/home/lotteryHistory?type=fcsd
   特点: 自带 next_code 字段，天然支持跨年期号回绕

2. 中彩网  【备份·常返回缓存页】
   类型: 网页解析（福彩官方）
   地址: https://www.zhcw.com/kjxx/fc3d/
   注意: 页面常为旧缓存(期号2022xx)，由 fetch_latest 期号校验自动拦截

已失效源（2026-08-01 实测确认，已从代码移除）：
  ✗ apihz    — HTTP 404
  ✗ 8200     — DNS解析失败 (域名已失效)
  ✗ 55128    — 连接被拒 (10061)
  ✗ 彩经网   — HTTP 403 Forbidden

防护机制：
  · fetch_latest 内置期号校验：凡返回期号 <= 本地CSV最新期号，一律视为
    缓存/旧数据拒绝采用，防止假数据污染历史。
  · 跨年安全：next_issue_calc() 优先用数据源 next_code，兜底按开奖日期
    12-31 判断回绕次年 001 期。

策略：依次尝试，哪个先返回"期号>本地"的新数据就用哪个。
云端cron每3小时（北京22:00/23:30/01:00）自动拉取一次。

⚠️ 风险提示：当前实际仅剩灰鸟API一个真活源，单点依赖。若灰鸟失效，
   云端将停止更新（但历史数据与回测不受影响，仅无新增期）。
