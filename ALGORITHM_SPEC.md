# 福彩3D 百十个杀码系统 V8 — 算法规格书

## 项目概述
福彩3D百位/十位/个位各杀一个数字（杀码），基于上期开奖号的非线性算术公式。
输入：上一期百位(b)、十位(s)、个位(g)，各∈[0,9]
输出：每个位置一个杀码（0-9中预测不开出的数字）
正确标准：杀码 ≠ 当期实际开出的该位置数字
随机基线：90%（10个数字杀1个，9/10概率蒙对）
目标：近200期综合准确率≥98%

## 整体架构
三位置各自独立算法，运行时可选"同昨日杀码回避"（回退队列fallback）。
版本演进：V7(固定条件树) → V8(十位增强V8a)

================================================================================

## 百位算法（10条件决策树）
版本：V7（未改动，稳定99.0%）
方法：条件优先级链，10条if-else分支，逐条匹配，匹配到就用对应公式。
设计逻辑：基于数字结构特征——奇偶组合、对子、跨度、大小关系。

### 条件决策树（从上到下匹配，命中即返回）
```
1. 全偶(b偶,s偶,g偶)          → 杀 = (b + s + g + 1) % 10
2. 全奇(b奇,s奇,g奇)          → 杀 = (b + s + g + 2) % 10
3. 百位=十位(b == s)          → 杀 = (3 × max(b,s,g)) % 10
4. 百位=个位(b == g)          → 杀 = (跨度 + 1) % 10       [跨度=max-min]
5. 十位=个位(s == g)          → 杀 = (b + s + g + 8) % 10
6. 跨度 == 4                  → 杀 = (b + s + g + 2) % 10
7. 跨度 >= 6                  → 杀 = (b × g - s) % 10
8. 和值为奇数(b+s+g奇数)       → 杀 = (b² + s + g²) % 10
9. 百位 < 个位(b < g)         → 杀 = (b + s + g + 2) % 10
10. 和值 <= 12                → 杀 = (跨度 + 3) % 10
11. 兜底                      → 杀 = (b + s + g + 1) % 10
```

### 回退队列（同昨日杀码回避）
当杀码==昨日同一位置杀码时，依次尝试：
- H_FALLBACK[0]: (b+s+g+1) % 10
- H_FALLBACK[1]: (b×s) % 10
都失败则 (杀码+1) % 10

### 性能
- 近100期: 99.0% | 近200期: 99.0% | 近500期: 94.4%
- 200期窗口内最大连错: 1期

================================================================================

## 十位算法（V8a增强公式法）
版本：V7→V8a升级
方法：3条件分支 + 2处V8a精确修复
核心升级：诊断出V7存在的两个结构性漏洞，针对性修复。

### V7原始算法（被V8a取代）
```
条件1: (b+s+g)%2==1              → 杀 = (b² + s² + g) % 10    [sum_odd分支]
条件2: 跨度>=6                   → 杀 = (3 × max(b,s,g)) % 10  [span_ge6分支]
兜底:                            → 杀 = (g² + b) % 10          [default分支]
```

### V8a升级（两处精确修复）

**修复1：sum_odd分支的 b²+s²=0 漏洞**
- 问题发现：当 (b²+s²) % 10 == 0 时（例如b=4,s=2 → 16+4=20尾0），原公式退化为杀码=g
  即杀码直接等于上期个位。若十位恰好等于个位（概率~10%），必错。
- V7在200期内3次因此漏洞出错（占总错误75%）。
- 修复：检测 (b²+s²)%10==0 → 切换为 (b+s+g+2)%10
- 新的条件1:
  ```
  if (b+s+g)%2==1:
      if (b*b + s*s) % 10 == 0:  return (b + s + g + 2) % 10
      return (b*b + s*s + g) % 10
  ```

**修复2：span_ge6分支百位最大时的次优公式**
- 问题发现：当跨度>=6且百位最大时，(3×max)%10 的出错率偏高（全量88.2%准确率）。
  尤其跨度=6,7时表现最差。
- 穷举20个备选公式后发现 ((b+s)×g)%10 在百位最大时表现最优。
- 修复：b最大 → ((b+s)×g)%10，其余 → 保持 3×max
- 新的条件2:
  ```
  if 跨度>=6:
      if b>=s and b>=g:  return ((b+s)*g) % 10
      return (3*max(b,s,g)) % 10
  ```

### 回退队列
T_FALLBACK: (g²+b)%10, (b+s+g+1)%10, 跨度, (b×g)%10, (b+s)%10, (b×s)%10

### 性能对比
| 版本 | 100期 | 200期 | 500期 | 1000期 | 全量 |
|------|-------|-------|-------|--------|------|
| V7   | 98.0% | 98.0% | 91.6% | 91.2%  | 90.19% |
| V8a  | 98.0% | 98.5% | 91.6% | 91.5%  | 90.37% |

================================================================================

## 个位算法（12条件决策树 + V8自适应备份）
版本：V7核心 + V8自适应备份机制
方法：12条件优先级链 + 失败追踪切换

### 条件决策树
```
1. 全奇                         → 杀 = (b+s+g+3) % 10
2. 百=十(b==s)                  → 杀 = (b+s+g+6) % 10
3. 百=个(b==g)                  → 杀 = (b+s+g+2) % 10
4. 十=个(s==g)                  → 杀 = (b+s+g+1) % 10
5. 跨度==4                      → 杀 = (b²+s²+g) % 10
6. 跨度==2                      → 杀 = (s×g+b) % 10
7. 个位最大(g==max)              → 杀 = (s×g+b) % 10
8. 百>个(b>g)                   → 杀 = (s×g) % 10
9. 有对子(任意两数相等)           → 杀 = (b×s+g) % 10
10. 和值>=15                    → 杀 = (b×s+s×g) % 10
11. 和值为偶数                   → 杀 = (s×g+b) % 10
12. 和值为奇数                   → 杀 = (g²×s) % 10
13. 兜底                        → 杀 = (s×g-b) % 10
```

### V8自适应备份机制
- 追踪每个条件分支最近一次失败的位置
- 当某条件在5期内失败过 → 切换到该条件的备份公式
- 备份公式映射:
  - g_max(个位最大): 3×max % 10
  - b_gt_g(百>个): b²+g % 10
  - sum_hi(和值≥15): b+s+g+3 % 10
  - sum_odd(和奇): b+s+g+1 % 10
  - default(兜底): b+s+g+1 % 10

### 回退队列
O_FALLBACK: (b+s+g+1)%10, (b×s)%10

### 性能
- 近100期: 97.0% | 近200期: 97.0% | 近500期: 92.6%

================================================================================

## 版本演进总结

### V1-V5: 统计法时代（已废弃）
- 频率分析、冷却机制
- 命中率天花板~95.17%
- 问题：统计信号不稳定，短期噪音淹没

### V6: 公式法突破
- 发现杀码算法.txt中的非线性算术公式思路
- 用上期三数字的平方、乘积、条件分支生成杀码
- 比统计法大幅提升

### V7: 固定条件决策树（主力框架）
- 百位10条件、十位3条件、个位12条件
- 每个位置独立设计条件链
- 回退队列避免连续同日杀码
- 200期综合98.0%

### V8: 十位V8a增强（当前版本）
- 不改变引擎架构，仅对十位做精确修复
- 修复1: sum_odd中b²+s²=0的结构性漏洞
- 修复2: span_ge6中百位最大时的次优公式
- 200期综合98.17%（提升+0.17pp，相当于多对1期）
- 已被证明无法进一步提升（穷举公式池/投票/加权/反共识等30+方法均失败）

================================================================================

## 已知限制与天花板

1. **长期收敛于~90%**: 全量8704期三个位置都在90.2%~90.6%，仅比随机基线高0.2~0.6pp。
   彩票本质上是随机过程，任何公式法在超长周期都无法超越随机太多。

2. **短期高命中率是可用的**: 100期(98%)和200期(98.2%)在统计上显著优于随机，
   实战价值在于近期窗口的稳定性。

3. **三位置全对天数**: 200期内同时全对率约85-90%，意味着有10-15%的天数至少错一个位置。

4. **最大连续错**: 200期窗口内综合最多连错3期，单位置最多连错1期。

================================================================================

## 数据结构

### CSV格式 (fc3d-history.csv)
```
issue,date,hundreds,tens,ones,number,raw
2026197,2026-07-26,2,3,2,232,"..."
```

### 引擎函数签名
```
kill_h(b, s, g) → 百位杀码 (int 0-9)
kill_t(b, s, g) → 十位杀码 (int 0-9)
kill_o(b, s, g) → 个位杀码 (int 0-9)
```
所有函数纯数学运算，无文件/网络/数据库依赖，输入仅上期b,s,g三个整数。
